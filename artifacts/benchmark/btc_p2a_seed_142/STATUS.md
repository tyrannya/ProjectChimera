# CURRENT

Frozen source run for the P2a model-family benchmark checkpoint.

Seed: 142

Research contract: `btc-usdt-1h-gen1`

This run carries current contract and research-input provenance, persisted outer
predictions, and did not evaluate the sealed test. Its deterministic simple-model
configurations produce zero seed dispersion, so this seed is not an independent
temporal confirmation — see `artifacts/README.md`.

Its aggregate interpretation is recorded in `artifacts/benchmark/btc_p2a_comparison`.
