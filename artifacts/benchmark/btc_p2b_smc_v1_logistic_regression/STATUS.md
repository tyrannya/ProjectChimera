# CURRENT

One P2b cell: the `logistic_regression` model on the `smc_v1` information set, over four temporal
outer folds, under the `btc-usdt-1h-gen1` research contract.

A source run for `benchmark/btc_p2b_comparison`, which is where the answer to the
research question lives. This directory on its own is one arm of a three-arm
comparison and says nothing about the question by itself.
