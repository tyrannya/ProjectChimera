# CURRENT

The authoritative result for the P2a model-family benchmark question: does model
complexity change what can be extracted from the same frozen OHLCV14 information set
MTST reads?

Generated from the five frozen `btc_p2a_seed_{42,142,242,342,442}` source runs, under
the `btc-usdt-1h-gen1` research contract, plus the frozen v4 MTST artifacts (read only,
not retrained).

This does not replace `artifacts/diagnostics/btc_regimes_v4` as the BTC OHLCV14/MTST
baseline — it answers a different research question. See `artifacts/README.md` for the
evidence index and interpretation.
