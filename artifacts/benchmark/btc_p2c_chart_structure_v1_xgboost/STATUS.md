# CURRENT

One P2c cell: the `xgboost` model on the `chart_structure_v1` information set, over four temporal
outer folds, under the `btc-usdt-1h-gen1` research contract.

A source run for `benchmark/btc_p2c_comparison`, which is where the answer lives. This
directory on its own is one arm of a three-arm comparison and says nothing by itself.
