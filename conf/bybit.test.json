{
    "$comment": "Dry-run (paper trading) on bybit. No orders reach the exchange. This is the default and recommended configuration.",
    "dry_run": true,
    "exchange": {
        "name": "bybit",
        "key": "${BYBIT_TEST_KEY}",
        "secret": "${BYBIT_TEST_SECRET}",
        "ccxt_config": {
            "enableRateLimit": true
        },
        "ccxt_async_config": {
            "enableRateLimit": true
        },
        "pair_whitelist": [
            "BTC/USDT"
        ],
        "pair_blacklist": []
    }
}
