{
    "$comment": "LIVE TRADING on binance. Real orders, real money. tools/run_bot.py refuses to start with this config unless ENABLE_LIVE_TRADING=I_UNDERSTAND_THE_RISK is set in the environment. Setting dry_run=false here is NOT sufficient on its own.",
    "dry_run": false,
    "exchange": {
        "name": "binance",
        "key": "${BINANCE_KEY}",
        "secret": "${BINANCE_SECRET}",
        "ccxt_config": {
            "enableRateLimit": true
        },
        "ccxt_async_config": {
            "enableRateLimit": true
        },
        "pair_whitelist": [
            "BTC/USDT"
        ],
        "pair_blacklist": []
    }
}
