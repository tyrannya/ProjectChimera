{
    "$comment": "HISTORICAL - not on the demo path (disconnected by PR-13, section 3.3). Dry-run (paper trading) on okx. No orders reach the exchange.",
    "dry_run": true,
    "exchange": {
        "name": "okx",
        "key": "${OKX_TEST_KEY}",
        "secret": "${OKX_TEST_SECRET}",
        "ccxt_config": {
            "enableRateLimit": true
        },
        "ccxt_async_config": {
            "enableRateLimit": true
        },
        "pair_whitelist": [
            "BTC/USDT"
        ],
        "pair_blacklist": []
    }
}
